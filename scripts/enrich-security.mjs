import { createClient } from '@supabase/supabase-js';
import { readFileSync } from 'fs';
const env=readFileSync(new URL('../.env.local',import.meta.url),'utf8');
for(const l of env.split('\n')){const m=l.match(/^([A-Z_]+)=(.*)$/);if(m)process.env[m[1]]??=m[2].trim();}
const sb=createClient(process.env.SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY);
const APPLY=process.argv.includes('--apply');

const ENRICH = {
206: `

## 국내 랜섬웨어 침해 동향

해외 통계만 보면 남의 일처럼 느껴지지만, 국내에서도 제조·의료·물류 기업을 노린 랜섬웨어 사고가 끊이지 않습니다. 특히 중견·중소 제조기업은 OT(생산설비) 네트워크와 IT 네트워크가 분리되지 않아, 사무망 감염이 곧바로 생산 중단으로 이어지는 사례가 많습니다. 병원의 경우 EMR(전자의무기록) 마비로 진료 자체가 멈추기 때문에 협상 압박이 극대화됩니다.

## 초기 침투 경로 Top 3

대부분의 랜섬웨어는 정교한 제로데이가 아니라 **방치된 기본기**를 파고듭니다.

| 침투 경로 | 비중(개략) | 핵심 차단책 |
|-----------|-----------|-------------|
| 노출된 RDP / VPN | 가장 높음 | 인터넷 직접 노출 차단, MFA 강제 |
| 피싱 메일 첨부 | 높음 | 첨부 샌드박싱, 매크로 차단 |
| 패치 안 된 외부 서비스 | 중간 | 외부 노출 자산 CVE 우선 패치 |

## 네트워크 세그멘테이션 (확산 차단)

랜섬웨어 피해 규모는 결국 **횡적 이동(lateral movement)을 얼마나 막느냐**로 결정됩니다.

\`\`\`
[사무망] —X— [서버망] —X— [OT/생산망]
   │              │             │
 최소 포트만   점프 호스트     단방향 게이트웨이
 허용(default deny)  경유 접근    (데이터 다이오드)
\`\`\`

- 평탄한(flat) 네트워크는 1대 감염이 전사 감염으로 직결됩니다.
- 관리자 계정의 공용 비밀번호 재사용을 금지하고 LAPS 등으로 로컬 관리자 암호를 무작위화하세요.

## 몸값 지불, 해야 할까

결론부터 말하면 **지불은 권장되지 않습니다.**

- 복호화 키를 받아도 완전 복구되는 비율은 절반에 못 미치는 경우가 많습니다.
- 지불 이력은 "지불하는 기업"으로 표적화되어 재공격을 부릅니다.
- 제재 대상(OFAC 등) 그룹에 지불 시 법적 문제가 생길 수 있습니다.

지불보다 **불변 백업에서의 복구 + 침투 경로 차단**이 정공법입니다.

## 국내 규제·신고 의무

- **정보통신망법**: 침해사고 발생 시 KISA(인터넷침해대응센터, 118)에 지체 없이 신고.
- **개인정보보호법**: 개인정보 유출이 동반되면 72시간 이내 정보주체 통지 및 신고.
- **ISMS-P**: 백업, 사고대응 절차, 로그 보존이 인증 통제 항목에 포함됩니다.

## 자주 묻는 질문 (FAQ)

**Q. 백업만 잘하면 EDR은 없어도 되나요?**
아니요. 백업은 최후의 복구 수단이고, 삼중 갈취(데이터 유출 공개)는 백업으로 막을 수 없습니다. 예방·탐지(EDR)와 복구(백업)는 별개 축입니다.

**Q. 클라우드를 쓰면 랜섬웨어에서 안전한가요?**
아니요. 잘못 설정된 S3/Blob, 탈취된 액세스 키로 클라우드 스토리지가 직접 암호화되는 사례가 늘고 있습니다. Object Lock(불변 보관)과 키 권한 최소화가 필수입니다.`,

207: `

## 로그 소스 온보딩 우선순위

SIEM은 "모든 로그를 다 넣는다"가 아니라 **탐지 가치가 높은 순서**로 연동해야 비용과 노이즈를 잡습니다.

| 우선순위 | 로그 소스 | 탐지 가치 |
|----------|-----------|-----------|
| 1 | 인증/계정 (AD, IAM, VPN) | 계정 탈취·권한 상승 |
| 2 | 엔드포인트 (EDR) | 악성 실행·횡적 이동 |
| 3 | 방화벽·프록시 | C2 통신·데이터 유출 |
| 4 | 클라우드 감사 로그 (CloudTrail 등) | 설정 변경·키 오남용 |
| 5 | 애플리케이션·DB | 비정상 쿼리·대량 조회 |

## Sigma — 벤더 독립 탐지 룰

특정 SIEM 문법(SPL/KQL)에 종속되지 않으려면 **Sigma 룰**로 작성해 각 SIEM 포맷으로 변환하는 방식이 유지보수에 유리합니다.

\`\`\`yaml
title: 단시간 다수 로그인 실패 후 성공
logsource:
  product: windows
  service: security
detection:
  failed:
    EventID: 4625
  success:
    EventID: 4624
  timeframe: 5m
  condition: failed | count() > 10 and success
level: high
\`\`\`

## 자체 구축 vs 매니지드(MSSP)

| 구분 | 자체 구축(In-house SOC) | 매니지드(MSSP) |
|------|------------------------|----------------|
| 초기 비용 | 높음 | 낮음(구독형) |
| 24/365 대응 | 인력 3교대 필요 | 포함 |
| 내부 맥락 이해 | 높음 | 낮음(튜닝 필요) |
| 적합 규모 | 대기업·금융 | 중견기업, 인력 부족 조직 |

전담 분석 인력을 3교대로 확보하기 어렵다면 MSSP로 시작해 핵심 역량만 내재화하는 단계적 접근이 현실적입니다.

## 비용 폭증 막기

클라우드 SIEM은 수집량(GB) 또는 EPS 기반 과금이라 **데이터 거버넌스가 곧 비용**입니다.

- 수집 단계에서 불필요 필드 드롭(예: 디버그 로그, 헬스체크).
- 핫(분석)/콜드(보존) 티어 분리 — 오래된 로그는 저비용 스토리지로.
- ISMS-P·전자금융감독규정 등 **로그 보존 기간 요건**을 먼저 확인하고 티어링을 설계하세요.

## 자주 묻는 질문 (FAQ)

**Q. SIEM만 도입하면 탐지가 자동으로 되나요?**
아니요. SIEM은 "기반"이고, 실제 탐지율은 상관분석 룰의 품질과 지속적 튜닝에 좌우됩니다. 룰 없이는 비싼 로그 저장소일 뿐입니다.

**Q. 오픈소스(Wazuh)로 시작해도 되나요?**
PoC와 중소 규모에는 충분합니다. 다만 대규모 수집 시 운영·확장 부담이 커지므로 성장 곡선을 보고 상용 전환을 검토하세요.`,

210: `

## 의존성 혼동(Dependency Confusion) 공격

사내 비공개 패키지명과 동일한 이름을 공개 레지스트리(npm/PyPI)에 더 높은 버전으로 올려두면, 빌드 도구가 공개본을 당겨가며 악성코드가 주입됩니다.

\`\`\`
방어:
- 사내 스코프(@company/...) 강제, 스코프 외 설치 차단
- 비공개 레지스트리 우선순위 고정 + 공개 폴백 비활성화
- 설치 무결성 검증(npm: --ignore-scripts, lockfile 검증)
\`\`\`

## 빌드 파이프라인 자체를 지키기 (SLSA)

SBOM이 "무엇이 들어갔나"라면, **SLSA**는 "그 빌드를 신뢰할 수 있나"를 다룹니다.

\`\`\`yaml
# GitHub Actions — 공급망 강화 핵심 3가지
- uses: actions/checkout@<full-40-char-SHA>   # 태그 대신 SHA로 고정
permissions:
  contents: read          # 최소 권한(default read)
  id-token: write         # OIDC 단기 토큰(장기 시크릿 제거)
\`\`\`

- 서드파티 액션은 **태그(v4)가 아니라 커밋 SHA로 고정**해야 태그 변조 공격을 막습니다.
- 장기 비밀키 대신 OIDC 기반 단기 토큰으로 클라우드에 인증하세요.

## VEX — "취약하지만 영향 없음"을 증명

SBOM에 CVE가 100개 붙어도 실제 악용 가능한 것은 일부입니다. **VEX(Vulnerability Exploitability eXchange)** 문서로 "이 CVE는 해당 함수를 호출하지 않아 영향 없음" 같은 판단을 기록하면, 무의미한 패치 압박과 고객 문의를 줄일 수 있습니다.

## 국내 맥락

- 과학기술정보통신부·KISA의 **소프트웨어 공급망 보안 가이드라인**이 SBOM 도입 기준을 제시합니다.
- 공공·금융 납품 시 SBOM 요구가 확대되는 추세이므로, CI 단계의 SBOM 자동 생성을 미리 표준화해 두는 것이 유리합니다.

## 자주 묻는 질문 (FAQ)

**Q. SBOM만 만들면 공급망 보안이 끝나나요?**
아니요. SBOM은 "재고 목록"일 뿐입니다. 신규 CVE 발표 시 기존 배포본을 소급 점검(continuous monitoring)하고 패치 SLA를 지키는 운영이 핵심입니다.

**Q. SPDX와 CycloneDX 중 무엇을 쓰나요?**
라이선스·컴플라이언스 중심이면 SPDX, 취약점 연동·보안 중심이면 CycloneDX가 편합니다. 도구가 둘 다 출력하는 경우가 많으니 소비처 요구에 맞추면 됩니다.`,

211: `

## 권한 관리가 곧 예방이다

탐지보다 앞선 방어는 **애초에 접근할 수 없게 만드는 것**입니다.

- **최소권한(PoLP)**: 직무에 필요한 최소 권한만 부여.
- **JIT(Just-In-Time) 접근**: 상시 관리자 권한을 없애고, 필요 시 승인 기반 임시 권한 부여.
- **정기 권한 재인증(Access Review)**: 분기별로 "이 사람이 아직 이 권한이 필요한가"를 소유자가 재승인.

## 퇴직자 오프보딩 체크리스트

내부자 사고의 상당수는 **퇴직 전후**에 집중됩니다.

\`\`\`
□ 퇴직 통보 시점부터 민감 시스템 접근 모니터링 강화
□ 마지막 근무일 계정 즉시 비활성화(삭제 전 보존)
□ VPN·SaaS·코드 저장소·클라우드 키 일괄 회수
□ 공용 계정 비밀번호 변경
□ 반출 기기·USB 회수 및 데이터 삭제 확인
\`\`\`

## SIEM/UEBA 연동 상관 룰 예시

\`\`\`
규칙: 퇴직 예정자 + 대량 다운로드
조건:
  user IN (HR.퇴직예정자_목록)
  AND download_volume > baseline * 3
  AND (destination = 외부저장소 OR USB)
액션: 즉시 보안팀·HR 공동 경보, 세션 기록
\`\`\`

## 국내 사례·법적 근거

기술 유출(특히 반도체·배터리 등 국가핵심기술)은 **산업기술보호법**과 **부정경쟁방지법**의 적용을 받습니다. 영업비밀 유출은 형사처벌 대상이며, 사전에 비밀관리성(접근통제·표시·서약)을 입증할 수 있어야 법적 보호를 받습니다.

## 자주 묻는 질문 (FAQ)

**Q. 직원 모니터링은 불법 아닌가요?**
업무용 IT 자원에 대한 모니터링은 **사전 고지와 취업규칙 명시**가 있으면 가능합니다. 다만 사생활(개인 메신저 등)까지 들여다보면 위법이 될 수 있으니 범위를 업무로 한정하고 법무팀과 협의하세요.

**Q. UEBA 없이 시작할 수 있나요?**
가능합니다. 우선 권한 최소화·로그 수집·퇴직자 오프보딩 같은 기본 통제부터 정착시키고, 이상행위 자동탐지가 필요해지면 UEBA를 도입하는 단계적 접근이 비용 효율적입니다.`,

213: `

## CVSS를 넘어: EPSS와 KEV

우선순위 결정에서 CVSS 점수만 보면 정작 악용되는 취약점을 놓칩니다. 두 지표를 함께 보세요.

- **CISA KEV(Known Exploited Vulnerabilities)**: 실제 공격에 사용된 것이 확인된 목록 → 최우선.
- **EPSS(Exploit Prediction Scoring System)**: 향후 30일 내 악용될 확률(0~1). CVSS 9.8이라도 EPSS가 낮으면 후순위가 될 수 있습니다.

\`\`\`python
# KEV + EPSS 결합 우선순위 (보강판)
def priority(cve):
    if cve.in_cisa_kev:          # 이미 악용 중
        return "긴급"
    if cve.epss >= 0.5 and cve.asset.internet_facing:
        return "높음"
    if cve.cvss >= 9.0:
        return "중간"
    return "정상 주기 패치"
\`\`\`

## 당장 패치할 수 없을 때: 보상 통제

운영 중단 위험·호환성 문제로 즉시 패치가 불가능한 자산도 많습니다. 이때는 **가상 패치(Virtual Patching)**로 시간을 법니다.

- WAF/IPS 시그니처로 해당 취약점 악용 트래픽 차단.
- 취약 서비스의 외부 노출 차단(접근 통제, 세그멘테이션).
- 보상 통제를 적용했더라도 **근본 패치 일정은 반드시 티켓으로 추적**.

## 컨테이너·클라우드 취약점 관리

\`\`\`bash
# 이미지 빌드 단계에서 차단 (CI에 통합)
trivy image --exit-code 1 --severity CRITICAL,HIGH my-app:latest

# IaC 설정 오류 점검
trivy config ./terraform
\`\`\`

이미지는 한 번 빌드하면 끝이 아니라, **레지스트리에 있는 기존 이미지도 신규 CVE 발표 시 재스캔**해야 합니다.

## 국내 규제 연계

- **ISMS-P**: 정기 취약점 점검과 조치 결과 관리가 인증 통제 항목입니다.
- **주요정보통신기반시설**: 연 1회 이상 취약점 분석·평가가 법적 의무(정보통신기반보호법).
- 점검만 하고 조치를 추적하지 않으면 인증 심사와 사고 대응 모두에서 문제가 됩니다.

## 자주 묻는 질문 (FAQ)

**Q. 스캐너가 너무 많은 취약점을 쏟아냅니다. 어디부터?**
KEV 목록 + 인터넷 노출 자산의 교집합부터 보세요. "악용 가능 × 노출됨"이 실제 위험입니다.

**Q. 패치하면 서비스가 깨질까 두렵습니다.**
그래서 테스트→스테이징→프로덕션 단계와 롤백 계획이 필요합니다. 패치 회피가 누적되면 그 자체가 가장 큰 위험이 됩니다.`,
};

let totalBefore=0, totalAfter=0;
for (const [id, add] of Object.entries(ENRICH)) {
  const {data,error}=await sb.from('posts').select('id,title,content').eq('id',+id).single();
  if(error){console.error(id,error.message);continue;}
  if(data.content.includes('자주 묻는 질문 (FAQ)')){console.log(`#${id} 이미 보강됨 — 건너뜀`);continue;}
  const next = data.content.trimEnd() + '\n' + add;
  totalBefore+=data.content.length; totalAfter+=next.length;
  console.log(`#${id} "${data.title.slice(0,30)}" ${data.content.length} → ${next.length}자 (+${next.length-data.content.length})`);
  if(APPLY){const{error:e}=await sb.from('posts').update({content:next}).eq('id',+id);if(e)console.error(e.message);}
}
console.log(`\n${APPLY?'적용 완료':'DRY RUN'}: 합계 ${totalBefore} → ${totalAfter}자`);
