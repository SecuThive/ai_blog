import { createClient } from '@supabase/supabase-js';
import { readFileSync } from 'fs';
const env=readFileSync(new URL('../.env.local',import.meta.url),'utf8');
for(const l of env.split('\n')){const m=l.match(/^([A-Z_]+)=(.*)$/);if(m)process.env[m[1]]??=m[2].trim();}
const sb=createClient(process.env.SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY);
const APPLY=process.argv.includes('--apply');

const E = {
217: `

## 멀티클라우드냐, 단일 클라우드냐

"세 개 다 쓰면 최고 아닌가?"라는 생각은 위험합니다. 멀티클라우드는 벤더 종속(lock-in)을 줄이고 가용성을 높이지만, **운영 복잡도·인력 역량·네트워크 비용**이 곱으로 늘어납니다.

- **단일 클라우드 권장**: 팀 규모가 작고 빠른 출시가 우선일 때. 관리형 서비스를 깊게 활용해 운영 부담을 줄이는 게 이득.
- **멀티클라우드 권장**: 규제상 특정 워크로드 분리가 필요하거나, 단일 벤더 장애가 사업에 치명적일 때. 단, 공통 추상화(Kubernetes, Terraform)로 이식성을 확보해야 함.

## 한국 리전·규제 체크포인트

글로벌 점유율만 보고 정하면 국내 요건에서 발목 잡힙니다.

| 항목 | 확인 포인트 |
|------|-------------|
| 리전 | AWS 서울, Azure 한국중부, GCP 서울 — 지연·데이터 주권 |
| 공공 | CSAP(클라우드 보안인증) 보유 서비스인지 |
| 금융 | 전자금융감독규정·망분리 요건 충족 여부 |
| 컴플라이언스 | K-ISMS-P, 개인정보 국외이전 동의 |

## 비용 함정 — 진짜 비싼 건 따로 있다

표의 컴퓨팅 단가보다 **이그레스(외부 전송) 요금**과 **관리형 서비스 프리미엄**이 청구서를 키웁니다. 데이터 전송이 많은 서비스라면 같은 클라우드 안에서 처리를 끝내도록 설계하고, 약정 할인(Savings Plans/CUD)은 사용 패턴이 안정된 뒤에 적용하세요.

## 자주 묻는 질문 (FAQ)

**Q. 결국 어디가 제일 좋나요?**
"제일 좋은 클라우드"는 없습니다. Microsoft 생태계·엔터프라이즈면 Azure, 데이터/ML 중심이면 GCP, 서비스 다양성·생태계면 AWS가 유리한 경향일 뿐, 결정 요인은 팀 역량과 비즈니스 요건입니다.

**Q. 락인이 무서운데 어떻게 줄이나요?**
컴퓨팅은 컨테이너(K8s), 인프라는 Terraform, 데이터는 표준 포맷(Parquet 등)으로 두면 이전 비용이 크게 줄어듭니다. 다만 관리형 서비스의 편의를 포기하는 트레이드오프가 있습니다.`,

65: `

## 가장 단순한 RAG, 코드로 보기

개념을 코드 한 조각으로 압축하면 다음과 같습니다. (의사코드)

\`\`\`python
# 1) 사전 준비: 문서를 청크로 쪼개 임베딩 → 벡터 DB에 저장
chunks = split(documents)            # 청킹
vectors = embed(chunks)              # 임베딩 모델
db.upsert(vectors, chunks)

# 2) 질의 시점
q_vec = embed(user_question)
ctx = db.search(q_vec, top_k=4)      # 의미 유사 청크 검색
answer = llm.generate(
    system="다음 컨텍스트만 근거로 답하라. 모르면 모른다고 답하라.",
    prompt=f"[컨텍스트]\\n{ctx}\\n\\n[질문]\\n{user_question}"
)
\`\`\`

핵심은 마지막 system 지시 — **"컨텍스트 밖의 내용은 지어내지 말라"**가 환각을 줄이는 첫 번째 안전장치입니다.

## RAG가 필요한 경우 vs 아닌 경우

| 상황 | 적합한 접근 |
|------|-------------|
| 사내 문서·최신 정보 기반 응답 | ✅ RAG |
| 말투·출력 형식 고정 | 파인튜닝(또는 프롬프트) |
| 단순 분류·추출 | 소형 모델 파인튜닝이 더 쌈 |
| 출처를 반드시 제시해야 함 | ✅ RAG (인용 가능) |

## 자주 묻는 질문 (FAQ)

**Q. RAG를 쓰면 환각이 완전히 사라지나요?**
아닙니다. 검색이 엉뚱한 문서를 가져오거나 컨텍스트가 불충분하면 여전히 환각이 발생합니다. 검색 품질(청킹·임베딩·리랭킹)이 곧 답변 품질입니다.

**Q. 파인튜닝과 RAG를 같이 쓸 수 있나요?**
네. 말투·도메인 적응은 파인튜닝으로, 사실·지식 주입은 RAG로 — 둘은 경쟁이 아니라 보완 관계입니다.`,

205: `

## ZTNA는 VPN을 어떻게 대체하나

제로트러스트의 네트워크 구현체가 **ZTNA(Zero Trust Network Access)**입니다. 전통적 VPN과의 차이가 핵심입니다.

| 항목 | VPN | ZTNA |
|------|-----|------|
| 접근 단위 | 네트워크 전체 | 애플리케이션 단위 |
| 신뢰 모델 | 한 번 인증 후 신뢰 | 매 요청 검증 |
| 횡적 이동 | 쉬움(같은 망) | 차단(앱별 격리) |
| 노출 | VPN 게이트웨이 IP 노출 | 앱 비공개(다크) |

VPN은 "터널 안이면 다 신뢰"하지만 ZTNA는 사용자·디바이스·앱 조합마다 정책을 평가합니다.

## 국내 도입 맥락

- KISA가 **제로트러스트 가이드라인 2.0**을 발간해 성숙도 모델·도입 단계를 제시합니다.
- 공공·금융은 망분리 규제와 함께 적용해야 하므로, ZTNA를 "망분리를 대체"가 아니라 "보완·현대화"하는 방향으로 설계하는 것이 현실적입니다.

## 흔한 실패 패턴

- **MFA만 도입하고 끝** — 디바이스 상태·컨텍스트 검증 없이 MFA만으론 제로트러스트가 아닙니다.
- **세그멘테이션 누락** — 신원 검증만 강화하고 횡적 이동(lateral movement) 차단을 빼면 침해 시 피해가 그대로 확산됩니다.
- **빅뱅 전환** — 한 번에 전사 적용하려다 업무 마비. 크리티컬 앱부터 단계적 적용이 정석입니다.

## 자주 묻는 질문 (FAQ)

**Q. 제로트러스트면 VPN을 당장 버려야 하나요?**
아니요. 대개 ZTNA와 VPN을 병행하다 앱 단위로 점진 전환합니다. 레거시 앱은 VPN에 남기고 신규/핵심 앱부터 ZTNA로 옮기는 식입니다.

**Q. 작은 회사도 제로트러스트가 필요한가요?**
규모와 무관하게 원칙(MFA·최소권한·로깅)은 유효합니다. 다만 풀스택 ZTNA 제품 대신 IdP(MFA)+클라우드 조건부 접근부터 시작하면 비용 효율적입니다.`,

268: `

## 모델을 작게 만드는 3가지 레버 (미리보기)

2편에서 깊게 다루지만, 엣지 배포의 성패는 **모델 경량화**에 달려 있어 핵심만 짚습니다.

| 기법 | 효과 | 트레이드오프 |
|------|------|-------------|
| 양자화(Quantization) | FP32→INT8로 크기 1/4, 추론 가속 | 미세한 정확도 손실 |
| 프루닝(Pruning) | 불필요 가중치 제거로 희소화 | 재학습 필요할 수 있음 |
| 지식 증류(Distillation) | 큰 모델 지식을 작은 모델로 이전 | 학습 파이프라인 복잡 |

대부분 INT8 양자화만으로도 정확도 1% 미만 손실로 4배 작아집니다.

## 엣지에서도 보안은 필수

디바이스가 물리적으로 노출되므로 **모델 자체가 탈취 대상**입니다.

- 모델 파일 암호화 및 보안 부트(Secure Boot)
- 추론 입력 검증(적대적 예제 방어)
- OTA 업데이트 채널의 서명 검증(위조 펌웨어 차단)

## 엣지 MLOps — 배포가 끝이 아니다

수천 대 디바이스에 흩어진 모델은 버전 드리프트가 생깁니다. 중앙에서 모델 버전·정확도·드리프트를 모니터링하고, 단계적 롤아웃(canary)과 자동 롤백 채널을 갖춰야 운영이 지속 가능합니다.

## 자주 묻는 질문 (FAQ)

**Q. 엣지와 클라우드 중 하나만 골라야 하나요?**
아니요. 실무에선 하이브리드가 많습니다 — 엣지에서 1차 추론(저지연), 애매한 케이스만 클라우드로 보내 정밀 분석하는 캐스케이드 구조가 비용·정확도 균형에 좋습니다.

**Q. 어떤 디바이스부터 시작하면 되나요?**
PoC는 Jetson이나 Coral 같은 개발 친화 보드로 시작해 추론 성능·전력을 측정한 뒤, 양산 단계에서 단가·수급을 고려해 결정하는 것이 안전합니다.`,

221: `

## 마이그레이션 자동화 도구

수작업 이전은 실수와 다운타임의 원인입니다. 워크로드별 전용 도구를 활용하세요.

| 도구 | 용도 |
|------|------|
| AWS MGN (Application Migration Service) | 서버 블록 단위 복제·리프트앤시프트 |
| AWS DMS | DB 이전(CDC로 무중단 동기화) |
| Azure Migrate | 평가·종속성 매핑·이전 통합 |
| Google Migrate to Containers | VM → 컨테이너 현대화 |

## 한국 환경 특수 고려사항

- **망분리·CSAP**: 공공/금융은 인증된 리전·서비스만 사용 가능. 사전 확인 필수.
- **데이터 주권**: 개인정보 국외 이전 시 동의·고지 요건. 국내 리전 우선 검토.
- **회선·이그레스**: 대용량 초기 이전은 전용선(Direct Connect/ExpressRoute)이나 물리 전송(Snowball)이 인터넷 전송보다 빠르고 쌀 수 있음.

## 마이그레이션은 결국 사람 문제

기술 이전보다 어려운 건 운영 조직의 변화입니다. 온프레미스 운영팀의 역할을 클라우드 운영(FinOps·SRE)으로 재정의하고, 교육·권한 체계를 함께 옮겨야 "이전은 했는데 아무도 운영 못 하는" 상황을 피합니다.

## 자주 묻는 질문 (FAQ)

**Q. 리프트앤시프트부터 하면 안 되나요?**
빠른 이전엔 유효하지만, 그 상태로 두면 클라우드 비용 이점을 못 누립니다. 이전 후 6R의 Replatform/Refactor로 단계적으로 최적화하는 계획을 함께 세우세요.

**Q. 비용이 오히려 늘었습니다. 왜죠?**
온프레미스 스펙 그대로 옮기면(과대 프로비저닝) 비싸집니다. 실제 사용량 기반 다운사이징, 약정 할인, 관리형 서비스 전환을 적용해야 절감이 실현됩니다.`,

218: `

## 시크릿 관리 — GitOps의 최대 난제

"모든 걸 Git에"가 원칙이지만 **비밀번호·키를 평문으로 커밋하면 안 됩니다.** 대표적 해법:

| 방식 | 특징 |
|------|------|
| Sealed Secrets | 공개키로 암호화해 Git에 저장, 클러스터에서만 복호화 |
| External Secrets Operator | Vault/AWS Secrets Manager 등 외부 저장소 참조 |
| SOPS + age/KMS | 파일 단위 암호화, 리뷰 가능 |

## 규모가 커지면 — App of Apps & 멀티클러스터

애플리케이션이 수십 개가 되면 ArgoCD Application을 또 다른 Application으로 관리하는 **App of Apps** 패턴이나 ApplicationSet으로 선언을 템플릿화합니다. 여러 클러스터(스테이징/프로드/리전별)도 단일 ArgoCD에서 타깃으로 관리할 수 있습니다.

## 드리프트 감지와 롤백

\`selfHeal: true\`는 클러스터에서 누군가 \`kubectl edit\`로 직접 바꿔도 Git 상태로 자동 복원합니다. 장애 시 롤백은 단순히 **Git revert** — 이전 커밋으로 되돌리면 ArgoCD가 동기화합니다. 배포 이력=Git 히스토리라는 GitOps의 가장 큰 장점입니다.

## 자주 묻는 질문 (FAQ)

**Q. ArgoCD와 Flux 중 무엇을 쓰나요?**
UI·멀티테넌시·App of Apps가 강점인 ArgoCD, 경량·GitOps 툴킷 조합이 강점인 Flux. 대시보드와 RBAC가 중요하면 ArgoCD가 진입장벽이 낮습니다.

**Q. selfHeal을 켜면 위험하지 않나요?**
긴급 수동 패치가 즉시 되돌려질 수 있으니, 운영 정책상 "변경은 항상 Git을 통해서만" 한다는 규율이 전제되어야 합니다. 그게 GitOps의 핵심이기도 합니다.`,

214: `

## OWASP LLM Top 10, 나머지 핵심 위협

프롬프트 인젝션 외에도 실무에서 자주 부딪히는 항목들입니다.

| 항목 | 위협 | 대응 |
|------|------|------|
| LLM03 학습 데이터 중독 | 오염된 데이터로 모델 편향·백도어 | 데이터 출처 검증·서명 |
| LLM04 모델 DoS | 과도한 토큰 요청으로 비용·가용성 공격 | 요청/토큰 레이트리밋 |
| LLM06 민감정보 노출 | 학습/컨텍스트의 기밀 유출 | 출력 필터·PII 마스킹 |
| LLM08 과도한 위임 | LLM에 과한 실행 권한 부여 | 행동 범위 최소화·휴먼 인 더 루프 |
| LLM09 과의존 | 검증 없는 출력 신뢰 | 사실 검증·인용 강제 |

## 가드레일과 레드팀

- **가드레일**: 입력/출력에 정책 필터를 두어 금지 주제·민감정보·프롬프트 탈취 시도를 차단.
- **레드팀**: 배포 전 의도적으로 인젝션·탈옥(jailbreak)을 시도해 취약점을 사전에 발견. 정기적으로 반복해야 합니다.

## 자주 묻는 질문 (FAQ)

**Q. 프롬프트 인젝션을 100% 막을 수 있나요?**
현재 기술로는 완전 차단이 어렵습니다. 그래서 "막는다"보다 **피해를 제한**하는 설계(최소 권한, 출력 검증, 민감 작업 휴먼 승인)가 핵심입니다.

**Q. 사내에서 ChatGPT 사용을 금지해야 하나요?**
전면 금지보다 가이드라인(기밀·개인정보 입력 금지, 승인된 도구·엔터프라이즈 플랜 사용)이 현실적입니다. 그림자 IT를 막으려면 안전한 대안을 제공하는 게 효과적입니다.`,

212: `

## 권한이 쌓이는 걸 막는 자동화

IAM은 "설정"보다 "지속 정리"가 어렵습니다. 권한 증식(permission creep)을 도구로 관리하세요.

| 클라우드 | 도구 | 역할 |
|----------|------|------|
| AWS | IAM Access Analyzer | 미사용 권한·외부 노출 탐지, 최소권한 정책 생성 |
| Azure | PIM Access Review | 관리자 권한 주기 재인증 |
| GCP | Policy Analyzer / Recommender | 과도 권한 추천 회수 |

## 멀티계정·조직 거버넌스

계정이 늘면 개별 IAM만으로는 통제가 안 됩니다.

- **AWS Organizations + SCP**: 조직 단위로 "이 리전/서비스는 아예 금지" 같은 가드레일 강제.
- **페더레이션/SSO**: IAM 사용자 남발 대신 IdP(OIDC/SAML) 연동으로 중앙 집중. 입·퇴사 시 한 곳에서 처리.
- **임시 자격증명**: 장기 Access Key를 없애고 STS·OIDC 단기 토큰으로 대체(키 유출 리스크 제거).

## 자주 묻는 질문 (FAQ)

**Q. 최소 권한을 어떻게 시작하나요?**
처음부터 완벽히 짜기 어렵습니다. 넓게 시작하지 말고, Access Analyzer로 **실제 사용된 권한 로그**를 기반으로 정책을 좁혀가는 접근이 현실적입니다.

**Q. 사용자마다 IAM 계정을 만들어야 하나요?**
권장하지 않습니다. IdP를 통한 SSO/페더레이션으로 중앙에서 신원을 관리하고, 클라우드에는 역할(Role)로 매핑하는 것이 보안·운영 모두 유리합니다.`,

209: `

## 코드만이 아니라 인프라(IaC)도 스캔하라

요즘 사고의 상당수는 애플리케이션 코드가 아니라 **잘못된 인프라 설정**(공개 S3, 열린 보안그룹)에서 납니다. 파이프라인에 IaC 스캔을 추가하세요.

\`\`\`yaml
      - name: Checkov IaC Scan
        uses: bridgecrewio/checkov-action@master
        with:
          directory: ./terraform
          framework: terraform
\`\`\`

tfsec, Terrascan도 같은 목적의 대안입니다.

## 공급망까지 — 빌드 산출물 신뢰

SAST/SCA로 끝이 아닙니다. 빌드마다 **SBOM(구성요소 목록)**을 생성하고 아티팩트에 서명(cosign)하면, 배포물이 변조되지 않았음을 검증할 수 있습니다.

## 보안 게이트, 현실적으로 운영하기

| 함정 | 현실적 운영 |
|------|-------------|
| Critical 전부 즉시 차단 | 도입 초기엔 신규 취약점만 차단, 기존분은 기한 부여 |
| 예외 없음 | 만료일 있는 예외(waiver) 절차로 빌드 마비 방지 |
| 알림 폭주 | High↑만 PR 코멘트, 나머지는 대시보드 |

처음부터 모든 게이트를 "차단"으로 켜면 개발팀이 우회로를 찾습니다. 점진 도입이 정착의 비결입니다.

## 자주 묻는 질문 (FAQ)

**Q. SAST·SCA·DAST 중 하나만 한다면?**
의존성 취약점이 사고의 큰 비중이라 **SCA**부터 시작하는 경우가 많습니다. 다만 셋은 보는 영역이 달라(코드/의존성/실행) 상호 보완적입니다.

**Q. 스캔 때문에 빌드가 너무 느려집니다.**
전체 스캔은 야간/머지 시점으로 돌리고, PR 단계에선 변경분 위주의 빠른 스캔(증분)만 돌리는 식으로 분리하면 개발 속도를 지킬 수 있습니다.`,
};

let before=0, after=0, n=0;
for(const [id, add] of Object.entries(E)){
  const {data,error}=await sb.from('posts').select('id,title,content,status').eq('id',+id).single();
  if(error){console.error(id,error.message);continue;}
  if(data.status!=='published'){console.log(`#${id} 발행상태 아님(${data.status}) — 건너뜀`);continue;}
  if(data.content.includes('자주 묻는 질문 (FAQ)')){console.log(`#${id} 이미 보강됨 — 건너뜀`);continue;}
  const next=data.content.trimEnd()+'\n'+add;
  before+=data.content.length; after+=next.length; n++;
  console.log(`#${id} "${data.title.slice(0,32)}" ${data.content.length} → ${next.length}자 (+${next.length-data.content.length})`);
  if(APPLY){const{error:e}=await sb.from('posts').update({content:next}).eq('id',+id);if(e)console.error(e.message);}
}
console.log(`\n${APPLY?'적용 완료':'DRY RUN'}: ${n}편, 합계 ${before} → ${after}자`);
